/**
 * Provider entity — re-exports shared cloud-provider model for FSD entity layer.
 */
export {
  CLOUD_PROVIDERS,
  DEFAULT_PROVIDER,
  PROVIDER_LABELS,
  isCloudProvider,
  type CloudProvider,
} from "../../shared/model/cloud-provider.ts";
